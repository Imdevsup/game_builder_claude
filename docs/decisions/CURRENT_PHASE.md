# Current phase

**Phase:** pre-Phase-0
**Status:** scaffolding complete, awaiting kickoff
**Owner:** human + tech-lead
**Target:** run `/start-phase 0` to begin.

## Definition of done for Phase 0

- [ ] Monorepo layout under `apps/` and `packages/` initialized.
- [ ] Shared types package compiles.
- [ ] Supabase project created. Initial schema migrated.
- [ ] Auth flow round-trips a login.
- [ ] Colyseus server accepts a WebSocket connection.
- [ ] Web client renders an empty world.
- [ ] CI pipeline green: lint, typecheck, test on every push.
- [ ] All 11 design templates remain valid (no contradictions introduced).

## Blockers

- None.

## Notes

This file is updated by the `producer` agent at the start and end of each phase. The previous phase's snapshot is archived under `docs/decisions/phases/`.
