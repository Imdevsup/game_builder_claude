---
description: kick off a balance pass
argument-hint: [area]
---

Balance pass area: $ARGUMENTS (default: all)

Run the balance-pass skill. Delegate to balance-analyst.

Pull current data, propose nudges with rationale, run balance-playtester for sanity check, ship behind a flag if uncertain.
